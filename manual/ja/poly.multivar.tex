\documentclass{report}

\input{../header_class}

%---------- start document ---------- %
 \section{poly.multivar -- 多変数多項式}\linkedzero{poly.multivar}
 \begin{itemize}
   \item {\bf Classes}
   \begin{itemize}
     \item \negok \linkingone{poly.multivar}{PolynomialInterface}
     \item \negok \linkingone{poly.multivar}{BasicPolynomial}
     \item \linkingone{poly.multivar}{TermIndices}
   \end{itemize}
 \end{itemize}

\C

 \subsection{PolynomialInterface -- 全ての多変数多項式の基底クラス}\linkedone{poly.multivar}{PolynomialInterface}
  インターフェースが抽象クラスなのでインスタンスは作らない.\\

  % No Documentation Yet
%
 \subsection{BasicPolynomial -- 多項式の基本的な実装}\linkedone{poly.multivar}{BasicPolynomial}
  基本的な多項式のデータ型.

  % No Documentation Yet
%
 \subsection{TermIndices -- 多変数多項式の項のインデックス}\linkedone{poly.multivar}{TermIndices}
  タプルのようなオブジェクト.
  \initialize
  \func{TermIndices}{\hiki{indices}{tuple}}{\out{TermIndices}}\\
  \spacing
  \quad コンストラクタは整数性や非負性などのインデックスの正しさを調べない.
  \begin{op}
    \verb/t == u/ & 等しいかどうか\\
    \verb/t != u/ & 等しくないかどうか\\
    \verb/t + u/ & (componentwise) 和\\
    \verb/t - u/ & (componentwise) 差\\
    \verb/t * a/ & (componentwise) スカラーによる積 {\tt a}\\
    \verb/t <= u, t < u, t >= u, t > u/ & 位数\\
    \verb/t[k]/ & {\tt k}番目のインデックス\\
    \verb/len(t)/ & 長さ\\
    \verb/iter(t)/ & イテレータ\\
    \verb/hash(t)/ & ハッシュ\\
  \end{op}
  \method
  \subsubsection{pop}\linkedtwo{poly.multivar}{TermIndices}{pop}
   \func{pop}{\param{self},\ \hiki{pos}{integer}}{(\out{integer}, \out{TermIndices})}\\
   \spacing
   % document of basic document
   \quad posにおけるインデックスとposのインデックスを除いた新しいTermIndicesオブジェクトを返す.

  \subsubsection{gcd}\linkedtwo{poly.multivar}{TermIndices}{gcd}
  \func{gcd}{\param{self},\ \hiki{other}{TermIndices}}{\out{TermIndices}}\\
  \quad 二つのインデックスの``gcd''を返す.

  \subsubsection{lcm}\linkedtwo{poly.multivar}{TermIndices}{lcm}
  \func{lcm}{\param{self},\ \hiki{other}{TermIndices}}{\out{TermIndices}}\\
  \quad 二つのインデックスの``lcm''を返す.

\C

%---------- end document ---------- %

\input{../footer}
